package com.brymian.boppo.damian.nonactivity;

public class __DOCUMENTATION {

    /*
     * VARIABLE ORDERING:
     * Variable are ordered according to the following criteria (objects towards the top of the list
     * are higher priority than those towards the bottom of the list):
     *
     * + Primary Key
     * + Alternate Key
     * + Importance
     * ---- Similarity
     *
     */
}
